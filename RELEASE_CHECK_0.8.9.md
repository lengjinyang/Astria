# Astria 0.8.9 发布检查

日期：2026-09-10。Windows x64，Electron 40.10.5，mpv 0.41.0。

## 已通过

- JavaScript 语法检查。
- 原生初始化取消、清理、重新打开，以及首帧状态回归。
- 工作区文件原子写入、缩略图缓存清理、帧缓存释放及媒体预热回归。
- 纯净模式、隐藏面板焦点隔离、关闭焦点返回、搜索与设置面板边界回归。
- 全新配置下首次打开 H.264 视频、自动播放、暂停、定位和截图。此次本机短素材测试主进程入口至首帧为 455 ms，至窗口完全显示为 776 ms；不代表所有机器与素材的速度。
- `npm run make:win -- --publish never`：NSIS 安装包与 ZIP 构建成功；重新构建原生模块，39 个运行时二进制无缺失 DLL。
- 发布包版本为 0.8.9；包内主要界面与播放源码与工作区一致。
- 直接运行 `out/builder/win-unpacked/Astria.exe --smoke-test`：实际播放、字幕与音轨、A/B 对比、GPU 示波器、拾色、批注和导出回归通过。
- 发布程序的真实分界线拖动、外部点击关闭、640 × 400 下示波器边界和焦点返回检查通过。
- 发布程序 PNG 序列缺帧保持、重新扫描新增帧、48 FPS 与书签恢复检查通过。

## 产物 SHA-256

- `Astria-Setup-0.8.9.exe`：`425d761fb208f269dc9f6ab62e15a81644eee39cd8046244917f40482e4fa90f`
- `Astria-0.8.9-x64.zip`：`e38fa94d0ef0da6108612ccccfe1e49bddfc3dbe2d4799b03bb960ae22be466b`

## 验证范围

安装包未签名；未执行安装、卸载、覆盖升级与干净机器验收。未重做全部格式、多显示器/DPI 和长时间双 4K 压力测试。日志仍有 Canvas 读回建议与 ResizeObserver 通知，但本轮回归断言全部通过。
