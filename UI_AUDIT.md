# 交互与视觉检查 · 2026-09-10

检查范围：桌面首页、视频常规/纯净模式、分析面板、工具菜单、音轨设置，以及自动化覆盖的字幕、A/B 对比、示波器与弹层关闭行为。不是对所有格式、DPI 和多显示器组合的完整验收。

## 本轮已修复

| 问题 | 调整 |
| --- | --- |
| 分析面板仅在视觉上隐藏，搜索框等控件仍出现在辅助功能树中并可获得键盘焦点 | 初始及收起状态设为 inert；打开后恢复交互，关闭时将内部焦点返回面板按钮 |
| 示波器关闭后焦点留在隐藏内容中 | 返回工具入口；处理标题栏已自动隐藏的情况，纯净模式下返回播放按钮 |
| 示波器无高度上限，小窗口下上缘及关闭按钮可能超出视口 | 按视口限制高度，允许内容滚动，固定关闭栏；常规与对比模式分别设置底部间距 |
| 桌面首页仍显示浏览器格式说明 | 根据运行环境区分说明，桌面说明包含 MXF 与图像序列 |
| 首页标题、说明与主操作的视觉层级偏弱，图标像文档 | 调整标题字号、行高、按钮间距及说明对比度，换为视频图标 |
| 新增工具字段的边框、底色、圆角与原有控件不统一 | 使用项目现有样式变量，统一字号、控件高度和悬停状态 |

## 仍建议改进的设计项

- 顶栏主要是 28 px 图标按钮，“工具”图标本身含义偏抽象。可在宽窗口给高频入口保留简短文字，或提供舒适密度选项。当前紧凑布局适合熟练用户，但首次使用需要更多悬停探索。
- 工具分类已在后续调整中完成：画面检查（亮度、拾色、色彩、构图）、对比分析（A/B 对比、示波器）、音轨与字幕（音轨、字幕）。使用常显分组标题与分隔线，保留原有快捷操作；浏览器版隐藏不支持的音轨与字幕分组。
- 半透明菜单叠在高对比内容上时，底层文字/画面会透入设置区域。后续可适度提高菜单不透明度，并统一各级菜单的次要文字对比度。
- 未载入视频时，亮度/拾色被禁用，但部分新增工具仍允许展开，音轨下拉框为空。建议统一“尚未载入”的说明与可用状态，同时保留能提前设置的偏好。

以上是设计建议，未在本轮整体重排导航或替换现有深色风格。

## 验证

- `npm run check`：通过。
- `scripts/clean-mode-smoke.cjs`：通过；补充隐藏面板无法获得焦点、打开可搜索、关闭返回入口的检查。
- 原生字幕回归：3 轮通过。
- Electron 播放与功能回归：通过；包括实际解码、字幕、GPU 示波器、A/B 模式及真实鼠标拖动。
- `scripts/release-ui-smoke.cjs`：通过；补充 640 × 400 窗口下常规/对比模式示波器边界和 Escape 焦点返回检查。
- 实际窗口复查：首页、隐藏面板辅助功能树、工具菜单和音轨字段。

本轮未修改解码或首帧启动链路，也未重新生成安装包。测试日志仍存在 Chromium 的 Canvas 读回性能提示和 ResizeObserver 提示；本轮没有将其归因于可见故障。
