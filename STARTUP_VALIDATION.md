# 首次打开视频优化验证

2026-09-10，Windows x64 开发版，Electron 40.10.5 / mpv 0.41.0。
每次使用全新的应用数据目录，无该视频的工作区或播放缩略图。
未清空操作系统文件缓存或显卡驱动缓存；以下为同机代表性单次结果，非性能保证。

| 素材 | 打开请求→首帧：修改前 / 后 | 主进程入口→窗口完全可见：修改前 / 后 |
| --- | --- | --- |
| H.264 320×180 | 346 / 193 ms | 823 / 774 ms |
| H.264 3840×2160 | 386 / 290 ms | 904 / 786 ms |
| HEVC 3840×2160 | 447 / 304 ms | 935 / 812 ms |

首帧阶段减少约 25–44%；整次进程启动到可见窗口减少约 6–13%。
该批测试在播放器内首次打开另一份 ProRes 素材约 249–277 ms。
提示延迟至 700 ms，快速加载直接出画；慢文件仍保留可取消的加载反馈。

根因与修改：

- `file-loaded` 中的同步属性查询阻塞首帧渲染回调，曾触发 mpv 的 200 ms VO 超时。
  现在先推进渲染，收到 `playback-restart` 再读取媒体信息，不呈现临时帧。
- 共享纹理直接绘制到同步的 sRGB Canvas2D，省去重复 WebGL 输出和每帧上传。
- 首帧取消淡入；用打开序号拒绝仍在传输的旧视频帧，保证快速重开时的续播位置。

通过的检查：

- JavaScript 语法、首帧渲染顺序、临时纹理释放、打开序号隔离、会话预热、帧缓存回归。
- 三种测试视频的无缩略图启动、自动播放、暂停、定位、打开另一视频和恢复 0.5 秒位置。
- 三种视频在 0.5 秒的 32×18 截图采样与修改前逐通道一致，最大差值为 0。
- 桌面播放功能检查：逐帧、字幕、音轨、速度、音量、原尺寸截图、批注、Contact Sheet、
  对比、示波器和界面交互。
- 1080p 灰阶素材在经典/纯净模式切换时的显示差值不超过 1 个通道值。
- PNG 序列缺帧处理、24→48 FPS、重新扫描后恢复工作区；浏览器 MP4 回退播放。

运行入口为 `npm run check:startup`。完整启动记录和采样位于 `.cache/startup-smoke-*/result.json`。
以上验证针对源码开发版；没有重建安装包。
